name: Build project and deploy to SmarterASP.NET

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    name: Build package and deploy to SmarterASP.NET
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Display current directory (for debugging)
        run: pwd

      - name: Display files in repository (for debugging)
        run: dir

      - name: Publish .NET Core Application
        run: |
          cd RentBuySell  # Change this to your actual project folder
          dotnet publish RentBuySell.csproj -c Release -o ../published_app

      - name: Display published files (for debugging)
        run: dotnet publish RentBuySell/RentBuySell.csproj -c Release -o published_app

      - name: Restart Website using MSDeploy
        run: |
          echo "Restarting the website..."
          # MSDeploy restart command for SmarterASP.NET
          # Using MSDeploy to restart the website (make sure the URL and credentials are correct)
          curl -u "krishnareddi1254-001:${{ secrets.FTP_PASSWORD }}" \
            -X POST "https://win1022.site4now.net:8172/msdeploy.axd?site=krishnareddi1254-001-site1"

      - name: Sync files to SmarterASP.NET
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: win1022.site4now.net
          username: krishnareddi1254-001
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: published_app/
          server-dir: /wwwroot/

      - name: Start the web server (if necessary)
        run: |
          echo "Starting the web server..."
          # In case you need to start the site via Web Deploy, you can add the start steps here.
          # Since we used MSDeploy to restart, this might be unnecessary.
